# 유가프로젝트하네스

작성일: 2026-06-02
목적: 유가 대시보드 프로젝트에서 `revfactory/harness`, `SaehwanPark/meta-harness`, `guiyoung2/harness_framework`를 어떻게 나눠 쓸지 정리한 대화 세션 export.

---

## 1. 핵심 결론

`revfactory/harness`는 기능을 바로 구현하는 도구라기보다 Claude Code에서 프로젝트별 에이전트 팀과 스킬 구조를 만들어주는 메타 하네스다.

유가 대시보드 프로젝트에서는 다음처럼 쓰는 것이 좋다.

```text
큰 방향/역할 분리/Phase 설계 = revfactory/harness
작은 기능 구현/반복 작업 실행 = guiyoung2/harness_framework
Codex 중심 운영 = SaehwanPark/meta-harness 또는 guiyoung2/harness_framework
```

`revfactory/harness`를 적용한다고 해서 기존 `guiyoung2/harness_framework`를 버릴 필요는 없다. 오히려 Harness는 초기 구조 설계와 큰 작업 분해에 쓰고, 기존 프레임워크는 평소 구현 루틴에 쓰는 조합이 현실적이다.

---

## 2. revfactory/harness의 성격

`revfactory/harness`는 Claude Code 전용에 가까운 팀 아키텍처 팩토리 플러그인이다.

설치 후 바로 로그인, 대시보드, 크롤링 기능을 구현하는 스킬이 생기는 것이 아니라 `harness`라는 메타 스킬이 생긴다. 이 스킬이 프로젝트를 읽고 다음과 같은 프로젝트 로컬 산출물을 만들 수 있다.

```text
.claude/agents/*.md
.claude/skills/*/SKILL.md
.claude/skills/*/references/*
CLAUDE.md
```

즉 전역 플러그인은 “하네스를 만드는 도구”이고, 실제 작업용 하네스는 각 프로젝트 안에 생성된다.

---

## 3. 자동 트리거와 토큰 소모 주의

`revfactory/harness`를 프로젝트에 세팅하면 `CLAUDE.md`, `.claude/skills`, `.claude/agents` 때문에 관련 요청에서 자동 트리거될 수 있다.

그래서 하네스 세팅 시 반드시 트리거를 좁게 잡아야 한다.

권장 문구:

```text
이 하네스는 사용자가 "하네스 모드"라고 명시할 때만 사용한다.
일반적인 기능 구현, 작은 버그 수정, 단순 UI 변경에는 자동 트리거하지 않는다.
```

나쁜 트리거:

```text
기능 구현, 버그 수정, 코드 작성, 리뷰할 때 사용
```

좋은 트리거:

```text
사용자가 "하네스 모드", "에이전트 팀", "다중 역할", "워크플로우 설계", "대형 작업 계획"을 명시할 때만 사용.
작은 기능 구현, 단순 수정, 단일 파일 변경에는 사용하지 않음.
```

---

## 4. 유가 대시보드 프로젝트에서 추천하는 하네스 구성

`oil_price_plan.md` 기준 Phase 1 MVP는 이미 잘 정의되어 있다.

핵심 목표:

```text
데이터를 다루는 프론트엔드
외부 API 연동
시계열 누적
대용량 지도/리스트 렌더링
시각화
의사결정 UX
```

처음부터 많은 에이전트를 만들지 말고 3~4개 정도로 시작한다.

추천 구성:

```text
1. orchestrator
   Phase 관리, 작업 분해, 범위 통제, 검증 기준 확인

2. data-pipeline
   Opinet API, Supabase, 좌표 변환, 스냅샷 적재, collection_logs
   Phase 2에서는 뉴스/RSS/API 수집까지 확장 가능

3. frontend-dashboard
   Vite React, 지도, 리스트, 차트, 즐겨찾기, UX

4. qa-performance
   Vitest/MSW, 접근성, Lighthouse, 대용량 렌더링 성능
```

처음부터 만들지 않을 에이전트:

```text
news-crawler-agent
rss-agent
html-parser-agent
seo-agent
content-moderation-agent
```

이들은 Phase 1에서는 과하다. 뉴스는 Phase 2 보조 기능으로 두고, 초기에는 `data-pipeline`의 하위 책임으로 기록만 한다.

---

## 5. 첫 Harness 세팅 프롬프트

Claude Code에서 `revfactory/harness`를 사용한다면 이렇게 시작한다.

```text
하네스 구성해줘.

C:/Users/귀영/Desktop/oil_price_plan.md를 기준으로
유가 대시보드 프로젝트의 Phase 1 MVP 구현 하네스를 설계해줘.

중요:
- 파일 생성 전 계획만 먼저 보여줘.
- 기본 팀은 3명 이하로 유지해줘.
- 일반 작업에 자동 트리거되지 않게 해줘.
- 사용자가 "하네스 모드"라고 명시할 때만 이 하네스를 사용하게 해줘.
- Phase 1 외 기능은 지금 만들지 마.
- 뉴스, 로그인, 웹푸시, 가격 알림은 보류로 둬.

필요한 역할:
- 데이터 수집/Supabase 담당
- 프론트엔드 대시보드 담당
- QA/성능 검증 담당

산출물:
- 생성할 .claude/agents 목록
- 생성할 .claude/skills 목록
- CLAUDE.md에 들어갈 최소 포인터
- Phase 1 작업 순서
- 각 단계별 검증 방법
```

계획이 마음에 들면 다음처럼 승인한다.

```text
계획 승인.
단, 자동 트리거는 좁게 유지하고,
"하네스 모드"라는 문구가 있을 때만 작동하도록 description을 작성해줘.
```

---

## 6. 구현 워크플로우

절대 한 번에 “MVP 전부 구현해줘”라고 하지 않는다.

나쁜 흐름:

```text
하네스 구성해줘
-> MVP 다 만들어줘
```

좋은 흐름:

```text
하네스 구성해줘
-> Phase 1 작업 단위 쪼개기
-> 1단계만 구현
-> 검증
-> 다음 단계
```

### Step 1. 프로젝트 스캐폴딩

```text
하네스 모드로 Phase 1의 1단계만 실행해줘.

범위:
- Vite + React + TypeScript 프로젝트 생성
- Tailwind, TanStack Query, Zustand, Vitest 기본 세팅
- 폴더 구조 잡기
- 아직 Supabase 연동 코드는 만들지 마

완료 기준:
- 기본 앱 실행 가능
- 테스트 러너 실행 가능
- 폴더 구조 설명
```

### Step 2. Supabase 스키마

```text
하네스 모드로 Supabase 스키마 작업만 진행해줘.

범위:
- stations
- price_snapshots
- collection_logs
- 필요한 인덱스와 unique constraint
- RLS는 읽기 정책 중심으로 최소 설정

완료 기준:
- SQL migration 작성
- 각 테이블 목적 설명
- price_snapshots 멱등 적재 가능
```

### Step 3. Opinet 수집 모듈

```text
하네스 모드로 collect-prices Edge Function 설계와 구현을 진행해줘.

범위:
- Opinet API 클라이언트
- 응답 파싱
- KATEC -> WGS84 변환 위치
- stations upsert
- price_snapshots insert
- collection_logs 기록
- 실패 시 partial/fail 기록

주의:
- 실제 API 키는 env로만 처리
- 응답은 MSW 또는 fixture로 테스트
```

### Step 4. 홈 화면

```text
하네스 모드로 홈 화면 MVP만 구현해줘.

범위:
- geolocation
- 지역 선택 fallback
- 카카오맵 표시
- 주변 주유소 리스트
- 유종 토글
- 가격/거리 정렬
- 마커 클러스터링은 가능하면 구현, 어렵다면 구조만 먼저

완료 기준:
- mock 데이터로 화면 확인
- 리스트 렌더링이 느려지지 않도록 가상 스크롤 고려
```

### Step 5. 상세 화면 + 차트

```text
하네스 모드로 주유소 상세 화면만 구현해줘.

범위:
- 현재가
- 가격 변동 차트
- 데이터 누적 전 empty state
- 즐겨찾기 localStorage 토글

완료 기준:
- 히스토리 데이터가 없을 때도 깨지지 않음
- 차트 접근성/로딩 상태 처리
```

### Step 6. 검증 패스

```text
하네스 모드로 Phase 1 QA 패스를 실행해줘.

범위:
- Vitest + RTL + MSW 테스트
- 좌표 변환 테스트
- 정렬/필터 테스트
- Lighthouse 측정 방법 정리
- 접근성 체크리스트
- 성능 병목 리포트

코드 변경은 필요한 최소 수정만 해줘.
```

---

## 7. 뉴스 및 정보 크롤링을 Phase 2로 둔 이유

뉴스/정보 크롤링을 미룬 이유는 중요하지 않아서가 아니다. Phase 1의 포트폴리오 신호가 더 강하기 때문이다.

Phase 1 핵심 신호:

```text
공공 API 데이터 수집
시계열 누적
Supabase/Postgres 설계
좌표 변환
지도/차트/가상스크롤
성능 측정
테스트/MSW
```

뉴스를 Phase 1에 넣으면 다음 작업이 추가된다.

```text
뉴스 API 선택
RSS 선택
HTML 파싱 여부
저작권/전문 복제 문제
요약 길이 제한
출처 표기
중복 기사 제거
수집 실패 처리
뉴스 섹션 UI
뉴스 필터링
```

따라서 뉴스는 Phase 2로 두는 것이 좋다.

Phase 2 기록:

```text
Phase 2 deferred:
- collect-news
- RSS 또는 네이버 검색 API
- HTML 파싱은 보조
- 전문 복제 금지, 제목/요약/링크만
- data-pipeline agent가 Phase 2에서 담당
```

뉴스 전담 에이전트가 필요한 시점:

```text
뉴스 소스가 3개 이상이다
RSS + 네이버 검색 API + HTML 파싱을 모두 쓴다
중복 제거/요약/출처 정책이 복잡하다
저작권/약관 검토가 필요하다
뉴스 수집 실패가 앱 운영에 중요한 영향을 준다
뉴스가 포트폴리오의 핵심 섹션이 된다
```

---

## 8. revfactory/harness와 SaehwanPark/meta-harness 차이

큰 개념은 비슷하다. 둘 다 도메인별 하네스를 만드는 메타 하네스다.

하지만 완전히 같은 도구는 아니다.

| 구분 | revfactory/harness | SaehwanPark/meta-harness |
| --- | --- | --- |
| 주 환경 | Claude Code | Codex 중심/런타임 중립 |
| 핵심 구조 | `.claude/agents/`, `.claude/skills/`, `CLAUDE.md` | `AGENTS.md`, `.agents/skills/`, `docs/harness/`, `_workspace/` |
| 실행 감각 | Claude Code Agent Teams 활용 | Codex가 읽기 쉬운 표준 문서/스킬/핸드오프 구조 |
| 목적 | Claude Code 네이티브 팀 아키텍처 생성 | 같은 개념을 Codex 친화적으로 이식 |
| 설치/적용 | Claude Code 플러그인/스킬 | Python installer와 layout 옵션 |

정리:

```text
Claude Code에서 Agent Teams를 쓸 거면 revfactory/harness
Codex 중심이면 SaehwanPark/meta-harness
기존 개인 루틴을 유지하려면 guiyoung2/harness_framework
```

---

## 9. 기존 guiyoung2/harness_framework와의 사용 구분

`revfactory/harness`를 적용한다고 기존 하네스 프레임워크를 버릴 필요는 없다.

추천 구분:

```text
revfactory/harness:
- 초기 역할 설계
- Phase 1 작업 분해
- 데이터 파이프라인/성능/QA처럼 복잡한 작업
- 큰 작업의 리서치/검증 구조 설계

guiyoung2/harness_framework:
- 평소 기능 구현 루틴
- 작은 UI 수정
- 단일 기능 추가
- 커밋 전 검증 흐름
```

예시:

```text
하네스 모드로 collect-prices 작업을 데이터 파이프라인/QA 관점으로 나눠서 진행해줘.
```

작은 UI 작업은 기존 프레임워크 사용:

```text
/harness 적용해서 주유소 카드 UI 개선해줘
```

---

## 10. 커밋 단위 추천

Phase 1은 단계별로 커밋한다.

```text
feat. 프로젝트 스캐폴딩 추가
feat. Supabase 유가 스키마 추가
feat. Opinet 가격 수집 함수 추가
feat. 주변 주유소 대시보드 추가
feat. 주유소 상세 차트 추가
test. 유가 대시보드 검증 보강
```

---

## 11. 최종 추천 진행 순서

```text
1. oil_price_plan.md 기반으로 Harness 계획만 생성
2. 자동 트리거 제한 조건 확인
3. 최소 에이전트 3~4개만 생성
4. Phase 1을 6개 작업 단위로 분리
5. 한 번에 한 Step만 구현
6. 각 Step마다 테스트/검증/커밋
7. Phase 1 완료 후에만 Phase 2 뉴스/동향 검토
```

핵심 원칙:

```text
작은 작업은 작게.
큰 작업만 하네스 모드.
뉴스는 보조 기능으로 Phase 2.
Phase 1은 데이터 파이프라인 + 지도/차트/성능에 집중.
```
