## 하네스: 유가 대시보드 계획 감리

**목표:** Phase 1 MVP 계획 작성, 범위 통제, 단계별 완료 기준 정의

**트리거:** 사용자가 "계획 하네스 모드"라고 명시할 때만 `planning-orchestrator` 스킬을 사용.
다음에는 사용하지 않음: 기능 구현, 버그 수정, UI 수정, 단일 테스트 추가, 문구 수정.

---

## 프로젝트 작업 루프

모든 세션은 작업 시작 전에 다음 파일을 먼저 읽는다.

1. .claude/loop/plan.md
2. .claude/loop/fix.md

`plan.md`는 현재 Phase, 진행 중인 Step, 다음 작업, 완료 기준을 기록한다.  
`fix.md`는 완료된 작업, 수정된 파일, 검증 결과, 남은 이슈를 기록한다.

작업이 끝나면 반드시 `plan.md`와 `fix.md`를 최신 상태로 갱신한다.

---

## 운영 규칙 (계획 하네스 모드)

- 계획 하네스 모드는 구현하지 않고 계획/검토/감리만 수행한다.
- Phase 1 범위(스캐폴딩 / Supabase 스키마 / collect-prices / 홈화면 / 주유소 상세 / QA)만 다룬다.
- 뉴스, 로그인, 웹푸시, 가격 알림은 Phase 2 보류로 즉시 분류한다.

---

## 커밋 메시지 규칙

모든 커밋은 아래 타입 접두사를 사용한다.

| 타입 | 용도 |
|------|------|
| `feat:` | 새로운 기능 추가 |
| `fix:` | 버그 수정 |
| `docs:` | 문서 추가/수정 |
| `refactor:` | 동작 변경 없는 코드 정리 |
| `test:` | 테스트 추가/수정 |
| `chore:` | 빌드 설정, 패키지, 기타 |
| `perf:` | 성능 개선 |
| `style:` | 포맷, 세미콜론 등 코드 스타일만 변경 |

예시: `feat: 주유소 목록 가상 스크롤 추가`

---

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-03 | 초기 구성 | 전체 | Phase 1 MVP 계획 전용 하네스 |
