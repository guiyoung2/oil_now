# 유가 대시보드 프로젝트 — 설계 문서 (oil_price_plan)

> 작성일: 2026-06-02
> 상태: **설계 확정 대기(유저 리뷰 단계)** — 승인 후 구현 계획(writing-plans)으로 진행
> 목적: 다른 세션에서 이 파일만 열어도 바로 이어서 작업할 수 있는 단일 핸드오프 문서

---

## 0. 다음 세션 시작 가이드 (먼저 읽기)

- 이 프로젝트는 **취업 포트폴리오용 신규 프로젝트**다. (현재 포트폴리오 레포와 별개의 새 레포로 만들 예정)
- 핵심 목표: **"데이터를 다루는 프론트엔드"** 라는 차별점 확보. 단, **크롤링 자체가 아니라 FE 엔지니어링(외부 API 연동·대용량 렌더 성능·시각화·의사결정 UX)이 주인공**이다.
- 제품 한 줄: **"현재 위치 주변 주유소의 현재가와 가격 변동 추이를 보여주고, 부가로 유가 동향·관련 뉴스를 제공하는 관망형 대시보드(개인화 한 겹 포함)."**
- 다음 액션: 이 문서의 §10 단계 계획에서 **Phase 1(MVP)** 부터 시작. 구현 전 `writing-plans` 스킬로 단계별 실행 계획 작성.

---

## 1. 배경과 의사결정 기록 (왜 이렇게 정했나)

취업 준비 중 서류 광탈이 지속되어 "데이터/대시보드/크롤링" 역량을 차별점으로 추가하려는 데서 출발. 시장 조사 결과 다음을 확인하고 방향을 조정했다.

- **크롤링은 FE 채용 신호가 약하다.** 크롤링(수집)은 백엔드/데이터 엔지니어링 전문 영역(Python/Scrapy 등). FE에서 가치 있는 건 "데이터를 화면에서 다루는 능력"이다.
- 따라서 **크롤링에 시간을 쏟지 않고**(공공 API로 충분), **FE 쇼케이스**로 설계한다. 뉴스 HTML 파싱은 "크롤링 경험" 한 줄을 위한 **보조 요소**일 뿐 핵심이 아니다.
- 2026 FE 시장이 실제로 보는 것: TypeScript, 성능 최적화, API 연동, 웹 접근성(a11y), AI 도구 활용, 중고신입(즉시 투입). 유저는 이 항목 대부분을 이미 보유.

### 도메인 선택 = 유가 (확정)
- 후보 비교 끝에 유가로 확정. 이유:
  - **100% 객관적 자동 데이터** — 가격은 사실(fact). 맛집/리뷰형의 "사용자 수동 입력 데이터" 함정이 없다.
  - **차별점 존재** — 오피넷 앱은 "현재가" 위주. "가격 변동 추이 + 개인화(내 지역/즐겨찾기) + (선택)알림"은 약하다. 여기가 틈새.
  - **FE 폭** — 시계열 차트 + 지도(전국 약 1.1만 주유소 → 마커 클러스터링 = 성능 과제) + 즐겨찾기 + PWA + 반경검색.
  - **합법** — 오피넷 공식 오픈API. 크롤링 차단/법적 리스크 없음.
- 제외한 후보: 부동산(흥미 없음), 음식점/상권(창업자 위주·흥미 낮음), 관광/숙박(차별점 부족), 쿠팡/야놀자/무신사(크롤링 ToS 위반·차단·"비교 쇼핑"으로 회귀).

### 제품 형태 = "지표 대시보드 + 개인화 한 겹" (B안, 확정)
- 메인은 "보는" 지표 대시보드(유저 본래 비전 유지). 단 "재밌지만 안 씀" 약점을 막기 위해 **"내 지역/즐겨찾기"라는 얇은 개인화**를 얹는다.
- "어디서/언제 넣어라"식 강한 추천 로직은 만들지 않는다(사람들은 단골/경로대로 가므로 ROI 낮음). 최저가 표시만 가볍게.

### 스택/아키텍처 결정
- **프레임워크: Vite + React** (Next.js 제외). 이유: 메인이 개인화·geolocation 화면이라 SSR/SEO 효용 낮음, 본질적으로 CSR, PWA 푸시는 Check Later 패턴 재활용, 수집 스케줄러는 Supabase가 담당하므로 Next.js 풀스택 이점 중복.
  - 단서: 부가 섹션(유가 동향/뉴스)의 검색 유입(SEO)을 적극 노린다면 Next.js도 합리적. 현재는 Vite 채택.
- **백엔드: Supabase** (확정). 이유: Postgres가 시계열 적재/집계에 최적(Firebase NoSQL 부적합), pg_cron 무료 스케줄러(Firebase는 유료 필요), RLS 보안, 유저 숙련도 높아 학습비용 0.

---

## 2. 목표 / 비목표

**목표**
- 외부 API 연동 + 매일 자동 수집 → 시계열 누적 → 의미 있는 시각화라는 **데이터 파이프라인**을 FE 중심으로 구현.
- 대용량(1.1만 주유소) 지도·리스트 렌더링 성능 최적화 쇼케이스.
- 접근성(WCAG)·테스트·성능 측정 등 기존 강점 유지.

**비목표 (YAGNI)**
- 강한 "주유 추천/최적 경로" 알고리즘.
- 결제·다중 사용자 협업 등 부가 비즈니스 기능.
- 거대한 데이터 볼륨 자랑(볼륨은 평가 지표가 아님).

---

## 3. 데이터 소스

### 3.1 오피넷 오픈API (메인, 합법)
- 무료 API 키(id) 발급 필요(오피넷 사이트).
- **좌표계 주의: 오피넷은 KATEC(TM) 좌표계 사용 → GPS(WGS84) 변환 필요.** (proj4 등) — "외부 API 연동의 까다로움" 어필 포인트.
- 제공 기능(정확한 엔드포인트/유종코드는 구현 시 공식 문서로 확정):
  - 좌표+반경 내 주유소 목록 + 가격 (주변 주유소)
  - 지역별 최저가 TOP
  - 전국/지역 평균가, 최근 추이(최근 N일)
  - 주유소 상세(고유 ID)
  - 지역코드/유종코드 목록
- **중요 제약: 주유소별 일별 히스토리는 오피넷이 제공하지 않는다.** → 우리가 매일 스냅샷을 적재해 시계열을 직접 누적한다. **이게 이 프로젝트의 데이터 엔지니어링 핵심.**
- 초기 한계(정직하게): 주유소별 상세 히스토리는 런칭 시점부터 누적되므로 처음엔 얇다. 거시 추이(전국/지역)는 오피넷 평균/추이 API로 즉시 표시 가능 → 빈 화면 회피.

### 3.2 뉴스 (보조, 크롤링 경험용)
- 합법 우선: **네이버 검색 API(뉴스)** 또는 **언론사 RSS**로 메타데이터(제목/링크/요약/일자) 수집.
- "진짜 HTML 파싱" 한 줄이 필요하면 허용되는 공개 페이지 1곳을 cheerio로 파싱(보조).
- 저작권: 전문 복제 금지. **제목 + 짧은 요약 + 원문 링크**만.

---

## 4. 아키텍처 개요

```
[pg_cron(매일)] → [Supabase Edge Functions]
                     ├─ collect-prices      : 오피넷 가격 수집 → upsert stations / insert price_snapshots
                     ├─ collect-regional-avg: 오피넷 평균·추이 수집 → regional_avg
                     └─ collect-news        : 뉴스 RSS/API(+보조 HTML 파싱) → news
                     (보류) notify-price     : 가격 변동 감지 → 웹푸시(VAPID) — 핵심 아님, 향후 선택
                          ↓ 적재
                   [Supabase Postgres + RLS]
                          ↑ 읽기 (TanStack Query)
[Vite + React PWA] ─ 카카오맵 · 차트 · 가상스크롤 · 즐겨찾기
```

- 앱은 오피넷을 직접 호출하지 않고 **우리 DB(Supabase)에서 읽는다.** (좌표변환·API키 노출·rate limit을 서버에서 흡수)
- API 키는 Edge Function 환경변수에만 둔다(클라이언트 노출 금지).

---

## 5. 데이터 모델 (Supabase Postgres)

- **stations** (주유소 마스터)
  - `id`(오피넷 고유 UNI_ID, PK), `name`, `brand`(폴사인), `address`, `lat`, `lng`(WGS84 변환 저장), `is_self`(셀프 여부), `updated_at`
- **price_snapshots** (일별 가격 스냅샷)
  - `id`, `station_id`(FK), `date`, `fuel_type`(휘발유/경유/고급휘발유/등유 등), `price`, `collected_at`
  - 유니크 제약: `(station_id, date, fuel_type)` — 멱등 적재
  - 인덱스: `(station_id, fuel_type, date)`
- **regional_avg** (전국/지역 평균·추이)
  - `id`, `region_code`(전국/시도), `date`, `fuel_type`, `avg_price`
- **news**
  - `id`, `title`, `url`, `source`, `summary`, `published_at`, `collected_at`
- **favorites** (보류 — 현재는 localStorage, 로그인 도입 시에만 DB화)
  - `user_id`, `station_id`, `created_at` — RLS: `user_id = auth.uid()`
- **price_alerts** (보류 — 알림 기능 도입 시에만)
  - `user_id`, `station_id`, `fuel_type`, `threshold`, `enabled`
- **collection_logs** (수집 신뢰성 — 운영 어필)
  - `id`, `job`(collect-prices 등), `started_at`, `finished_at`, `status`(success/partial/fail), `rows`, `error`

---

## 6. 화면 구성

1. **홈 — 내 주변** (메인)
   - Geolocation → 카카오맵 + 주변 주유소 마커(**클러스터링**) + 가격 라벨
   - 주유소 리스트(**가상 스크롤**), 유종 토글(휘발유/경유/LPG 등), 정렬(거리/가격)
   - 최저가 가볍게 강조(추천 로직은 과하게 X)
2. **주유소 상세**
   - 현재가 + **가격 변동 차트**(우리 누적 시계열) + 즐겨찾기 토글
3. **유가 동향** (부가)
   - 전국/지역 추세 차트, 유종 비교
4. **뉴스** (부가)
   - 유가 관련 뉴스 피드(제목/요약/링크)
5. **즐겨찾기**
   - 내 주유소 모음 + 현재가 (Phase1: localStorage / Phase3: 로그인 동기화)
6. **로그인** (선택, Phase 3) — Supabase Auth

---

## 7. 데이터 흐름

- **수집(쓰기):** pg_cron → Edge Function → 오피넷/뉴스 호출 → upsert/insert → collection_logs 기록
- **조회(읽기):** 앱 → TanStack Query → Supabase(우리 DB)에서 주변 주유소(최신 스냅샷)·상세 히스토리·동향·뉴스
- **알림(보류):** 도입 시 notify-price Edge Function이 가격 변동 감지 → 즐겨찾기 임계값 충족 시 웹푸시(VAPID, Check Later 패턴 재활용). 현재는 핵심 아니므로 미구현.

---

## 8. 에러 처리 / 운영

- 오피넷 호출 실패·타임아웃 → **재시도(지수 백오프)**, **부분 실패 허용**(지역 순회 수집이라 일부 지역 실패해도 나머지 적재), collection_logs에 partial 기록
- Geolocation 거부/실패 → **지역 선택 fallback**(시/도 드롭다운)
- 빈 히스토리(런칭 초기) → "데이터 누적 중" 안내 UI
- 뉴스 소스 구조 변경 → 파싱 실패 graceful skip(앱 전체 영향 없음)

---

## 9. 비기능 요구 (강점 축)

- **성능:** 1.1만 마커 클러스터링 + 뷰포트 기반 렌더 / 긴 리스트 가상 스크롤 / 차트 다운샘플링 / 부가 섹션 코드 스플리팅(lazy) / **Lighthouse 전후 측정 기록**
- **접근성:** WCAG AA(터치 타깃 ≥44px, 키보드 내비, 대비) — 기존 강점 유지
- **보안:** RLS(favorites/alerts `user_id=auth.uid()`), API 키는 Edge Function 환경변수에만
- **테스트(Vitest + RTL + MSW):** 수집 파서 단위(오피넷 응답 모킹), 좌표 변환, 컴포넌트(정렬/필터)

---

## 10. 단계 계획 (MVP → 확장)

### Phase 1 — MVP (가장 먼저)
- Supabase 프로젝트 + 스키마(stations, price_snapshots, collection_logs)
- 수집 파이프라인: `collect-prices` Edge Function + pg_cron(매일)
- 홈(내 주변): 카카오맵 + 주변 주유소 마커/리스트(클러스터링·가상스크롤·유종 토글·정렬)
- 주유소 상세: 현재가 + 가격 변동 차트(누적분)
- 즐겨찾기: **localStorage**(로그인 없이)
- 기본 테스트 + Lighthouse 측정
- ✅ 완료 기준: 매일 자동 수집이 돌고, 주변 주유소·상세 히스토리가 화면에 뜨고, 테스트/성능 측정이 존재

### Phase 2 — 부가 섹션
- 유가 동향(전국/지역 추세, 유종 비교) — `collect-regional-avg`
- 뉴스 수집/섹션 — `collect-news`(RSS/검색API + 보조 HTML 파싱)

### 보류 (향후 필요 시에만 — 현재 비핵심)
- Supabase Auth 로그인 + 즐겨찾기 DB 동기화(RLS) — 기기 간 동기화가 필요할 때만. localStorage로도 데모·포트폴리오에 충분.
- 가격 변동 웹푸시 알림(VAPID) — `notify-price`, `price_alerts` 테이블. 도입 시 Check Later 패턴 재활용.

---

## 11. 구현 단계에서 확정할 미결 사항

- 오피넷 정확한 엔드포인트·유종코드(공식 문서 확인)
- 좌표 변환 방식(proj4 vs 오피넷 제공 좌표 활용)
- 전국 1.1만 주유소 일괄 수집 방법(지역코드 순회 vs 반경 누적) + Edge Function 실행시간 한도 대응(배치 분할)
- 뉴스 소스 최종 확정(네이버 검색 API 키 발급 vs 언론사 RSS)
- 프로젝트/도메인 이름 (예: OilNow, 오늘의주유 등 — 미정)

---

## 12. 기술 스택 요약

Vite, React 19, TypeScript, Tailwind CSS, TanStack Query, Zustand, Supabase(Postgres/Auth/Edge Functions/pg_cron/RLS), vite-plugin-pwa(웹푸시), 카카오맵 SDK, 차트 라이브러리(미정: Recharts/visx 등), Vitest + Testing Library + MSW, Vercel(배포)
