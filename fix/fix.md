# Fix 추적 — oil_now

> Phase 1 구현 중 발견된 이슈 및 수정 기록
> 마지막 갱신: 2026-06-13 (앱 메타데이터 S20)

---

## 미결 이슈

| # | 유형 | 발견 단계 | 심각도 | 상태 | 메모 |
|---|------|----------|-------|------|------|
| 17 | `pipeline` | Phase 2-A | minor | ✅ 해소 | `collect-regional-avg` pg_cron 스케줄 활성화 완료(2026-06-12). 사용자가 pg_cron 확장 활성화 후 Supabase MCP로 `cron_collect_regional_avg` migration 재적용. `collect-regional-avg-daily` 잡 등록(`30 1 * * *`, 매일 10:30 KST). |
| 18 | `pipeline` | Phase 2-B S18 | note | ✅ 해소 | Deno 런타임에 `DOMParser` 미제공. `parseNews.ts` v1(DOMParser 기반)을 정규식 기반 XML 파싱으로 교체. Vitest(jsdom)와 Deno 모두 동작. |
| 19 | `pipeline` | 2026-06-13 | critical | ✅ 해소(S21) | pg_cron Vault 시크릿 미등록으로 자동 수집 전량 실패. `functions_url`·`service_role_key` 모두 미등록 상태여서 `net.http_post` null URL 오류 발생. MCP로 두 시크릿 등록 완료. `collect-regional-avg` v3(avgRecentPrice.do+avgAllPrice.do 조합)으로 교체 배포 + 6/6~6/13 40행 백필. |
| 20 | `pipeline` | 2026-06-14 (S30) | minor | ✅ 해소(S31) | regional_avg `diff`(전일대비) 다수 0.00 + 당일 데이터 부재. **원인 3종 분리:** (1) avgRecentPrice.do 실응답에 DIFF 필드 없음→과거일 전부 0 (수정: 유종별 인접일 차이 자체 계산). (2) cron 자동 적재가 6/12·6/13 Vault 시크릿 NULL로 실패했으나 시크릿은 S21에 이미 등록 완료→수동 동일 경로 트리거로 http 200·success 복구 검증. (3) 당일 데이터 부재는 Opinet API가 당일 미제공(전일 기준)이라 불가피=정상. 재배포(v4)+트리거 후 6/8~6/14 diff 인접일 차이로 정확 적재 확인. |

| 1 | `schema` | 계획 감리 | minor | ✅ 결정 | `price_snapshots.fuel_type` → `TEXT` 타입 채택. API 확인 전 안전한 선택. 숫자 코드 확인 시 마이그레이션으로 변경. |
| 2 | `schema` | 계획 감리 | minor | ✅ 결정 | `stations.updated_at` 자동 갱신 트리거 추가. Supabase `moddatetime` 확장 사용. |
| 3 | `pipeline` | 계획 감리 | minor | ✅ 해소 | 엔드포인트·유종코드·응답필드·Key 모두 확인 완료. Base URL: https://www.opinet.co.kr/api/ / 엔드포인트: lowTop10.do(시도별 최저가 TOP20), aroundAll.do(반경 주유소), detailById.do, avgAllPrice.do / 유종코드: B027(휘발유) D047(경유) B034(고급휘발유) C004(등유) K015(자동차부탄) / 시도코드: 01~19(18개) / 응답필드: UNI_ID OS_NM NEW_ADR POLL_DIV_CD(브랜드) GIS_X_COOR GIS_Y_COOR PRICE / 좌표: KATEC→WGS84 변환 필요 / 일일한도: 1,500회 / 수집전략: lowTop10.do cnt=20 per 시도(18) per 유종(5) = 90회/일 |
| 4 | `pipeline` | 계획 감리 | minor | ✅ 결정 | 전국 수집 전략: 시도 17개 순차 순회(A). pg_cron → 함수 1개 → 시도별 루프. 속도 문제 시 Phase 2에서 지역별 병렬 분할로 개선. 일일한도 1,500회/API키 감안하여 유종 1개씩 per 시도 요청 설계. |
| 5 | `ui` | 계획 감리 | minor | ✅ 결정 | 카카오맵 SDK → `index.html` 정적 스크립트 태그 방식 채택. 공식 가이드 방식, 단순. 성능 최적화(동적 로드)는 Phase 2 검토. |
| 6 | `ui` | 계획 감리 | minor | ✅ 결정 | 즐겨찾기 목록 화면 → Phase 1 제외. 로그인 없는 MVP에서 목록 관리 불필요. 각 주유소 상세 화면에 ☆ 토글 버튼만 포함. 목록 화면은 Phase 2 보류. |
| 7 | `pipeline` | Step 2 검토 | minor | ✅ 결정 | price 0·결측 처리: 수집 시 price ≤ 0 행 스킵(적재 안 함). Step 3 완료 기준에 명시. |
| 8 | `pipeline` | Step 2 검토 | minor | ✅ 결정 | fuel_type 정규화: 오피넷 코드 → 앱 내부 코드 변환. 매핑: B027→gasoline / D047→diesel / B034→premium / C004→kerosene / K015→lpg. |
| 9 | `pipeline` | Step 2 검토 | minor | ✅ 결정 | date KST 기준: 수집 함수에서 now() AT TIME ZONE 'Asia/Seoul' 으로 date 계산. |
| 10 | `pipeline` | Step 2 검토 | minor | ✅ 결정 | collection_logs: 단일 행 관리(job 시작 시 INSERT, 종료 시 UPDATE). error TEXT에 실패 시도 목록 JSON 문자열로 기록. partial 기준: 1개 이상 시도 실패 시. |
| 11 | `pipeline` | Step 3 검토 | major | ✅ 해소(S14) | 좌표 변환 단일 출처화 완료(2026-06-12). 본문을 `supabase/functions/_shared/coord.ts` 한 곳으로 통합. Edge Function 2개는 `../_shared/coord.ts` import, `src/lib/coord.ts`는 re-export 배럴. 테스트 7/7·전체 71/71·build 통과. **단 Edge Function 재배포 필요(잔여 2와 함께)** — 현 배포본은 옛 인라인 코드(로직 동일). |
| 12 | `pipeline` | Step 3 검토 | critical | ✅ 해소 | pg_cron 마이그레이션의 service_role 키 평문 저장 문제. Supabase Vault 시크릿 참조 방식으로 전환 완료. git 커밋 안전. |
| 13 | `pipeline` | Step 3 검토 | critical | ✅ 해소 | collection_logs status 오분류: API HTTP 200이나 rows=0이어도 success 기록 문제. totalRows===0 → partial, DB write 에러 failures에 반영 완료. |
| 14 | `pipeline` | Step 3 검토 | major | ✅ 해소(S10,S15) | aroundAll.do 실호출 검증 완료(2026-06-11). 응답 경로 `RESULT.OIL[]` 확인. aroundAll.do 응답 필드: UNI_ID/OS_NM/POLL_DIV_CD/PRICE(숫자)/DISTANCE(m)/GIS_X_COOR/GIS_Y_COOR. lowTop10.do(collect-prices)도 실호출 검증 완료(2026-06-12, S15): 응답 경로·cnt=20 정상, **단 지역 파라미터는 siGunGu가 아니라 `area` → #16 참조.** |
| 16 | `pipeline` | 잔여 2 검증 | major | ✅ 해소(S15) | lowTop10.do 지역 파라미터 결함. `siGunGu`는 무시되어 전국 최저가 TOP20만 반복 반환(17시도 → 고유 104행만 적재). 실호출로 `area`가 올바른 파라미터임을 확정(area=01 서울, area=11 제주로 명확히 구분). `siGunGu`→`area` 수정 후 재배포·재실행: 104→1,667행, 고유 주유소 1,356개로 정상화. 영향도 낮음(price_snapshots는 현재 화면 미사용 — nearby는 aroundAll 실시간, 대시보드는 mock). |
| 15 | `pipeline` | Step 3 검토 | major | ✅ 해소 | Edge Function 실행시간 한도 초과 위험. 조합 단위 배치 upsert로 DB 왕복 수 대폭 감소(N→2/조합). 배포 후 실행시간 실측 권장. |

---

## 완료된 수정

| # | 설명 | 수정된 파일 | 검증 결과 | 완료일 |
|---|------|-----------|----------|-------|
| S1 | Step 1 스캐폴딩 완료 — Vite 8 + React 19 + TS, Tailwind v4, TanStack Query/Virtual, Zustand, Recharts, Vitest, MSW, vite-plugin-pwa 설치 및 기본 폴더 구조 생성 | package.json, vite.config.ts, src/index.css, src/App.tsx, tsconfig.app.json | npm run dev ✅ / vitest run 1 passed ✅ / tsc --noEmit ✅ | 2026-06-10 |
| S2 | Step 2 Supabase 스키마 완료 — stations/price_snapshots/collection_logs 테이블 생성, unique constraint (station_id,date,fuel_type), 인덱스, RLS 정책(읽기 공개/쓰기 service_role), moddatetime 트리거 | Supabase migration: create_phase1_schema | 테이블 3개 ✅ / unique constraint ✅ / 인덱스 ✅ / RLS 6개 정책 ✅ / 멱등 upsert ✅ | 2026-06-10 |
| S3 | Step 3 collect-prices Edge Function 구현 — KATEC→WGS84 순수 수학 변환, 오피넷 파서, 배치 upsert, collection_logs 3-status, Vault 기반 pg_cron 마이그레이션 | supabase/functions/collect-prices/index.ts, src/lib/coord.ts, src/lib/parseOpinet.ts, src/test/coord.test.ts, src/test/parseOpinet.test.ts, supabase/migrations/20260610000001 | vitest 10/10 ✅ / tsc --noEmit ✅ / 미결: #14(API 파라미터 실호출 검증), #11(coord 코드 중복) | 2026-06-10 |
| S4 | Vercel 배포 빌드 실패 수정 — `vite.config.ts`의 `test` 설정 타입이 Vite `defineConfig`에서 인식되지 않던 문제를 Vitest config helper로 전환 | vite.config.ts, plan/plan.md, fix/fix.md | npm run build ✅ / npm run test -- --run ✅ | 2026-06-10 |
| S5 | Task 4 Zustand filterStore 구현 — TDD 방식. RED: 테스트 파일 생성, FAIL 확인. GREEN: 최소 코드로 3 tests PASS. 초기값(gasoline, distance), setFuelType, setSortOrder | src/store/filterStore.ts, src/test/filterStore.test.ts | vitest 3/3 PASS ✅ / npm run test 전체 16/16 PASS ✅ / git commit 230efa0 ✅ | 2026-06-10 |
| S6 | Step 4 홈 화면 구현 완료 (서브에이전트 11-Task) — react-router-dom/supabase 설치, 타입/유틸, MSW, filterStore, useGeolocation/useStations, FilterBar/StationCard/EmptyState/StationList/KakaoMap, HomePage + App.tsx 조립 | 17개 신규 파일 + index.html, src/main.tsx 수정 | 10 files 35 tests ✅ / tsc --noEmit ✅ / 커밋 6a5f5d9 | 2026-06-10 |
| S7 | 카카오맵 로딩 실패 진단·해소 — 신규 앱은 2024.12 정책상 카카오맵 미심사라 SDK 403(disabled OPEN_MAP_AND_LOCAL). 개발용으로 이미 권한 보유한 기존 앱(VibeBoard) JS 키 재사용으로 즉시 해소. KakaoMap을 `kakao.maps.load()` 콜백 패턴으로 보강, MSW localhost 노이즈 억제 | .env.local, src/components/home/KakaoMap.tsx, src/main.tsx | curl Referer 200 + SDK 본문 확인 ✅ / 브라우저 지도 표시 ✅ | 2026-06-11 |
| S8 | Step 4.5 탭 네비게이션 + 실시간 유가 mock 대시보드 — 상단 3탭(실시간 유가/주변 주유소/유가 뉴스), 첫 화면=실시간 유가, 주변 주유소=`/nearby`. AvgPriceCard·PriceTrendChart(Recharts)·useAvgPrices(mock)·avgPriceFixtures. TDD(RED→GREEN) 5개 단위. 부수: build 차단하던 기존 `global` 타입 에러(S6 유입, tsc --noEmit만 검증돼 미발견)를 `globalThis`로 수정 | 신규 8개(TabBar/MainLayout/PricesPage/NewsPage/AvgPriceCard/PriceTrendChart/useAvgPrices/avgPriceFixtures + types/avgPrice) / 수정 App.tsx·HomePage·smoke.test·useGeolocation.test | 14 files 45 tests ✅ / tsc --noEmit ✅ / npm run build ✅ | 2026-06-11 |
| S9 | dev 콘솔 정리 + 주변 주유소 리스트 표시 — (1) MSW handler가 요청 위치(lat/lng 범위)를 파싱해 fixtures를 그 주변으로 평행이동 → 서울 밖에서도 리스트/마커 표시. (2) PriceTrendChart를 ResponsiveContainer→ResizeObserver 직접 측정으로 변경해 width(-1) 경고 제거(+jsdom ResizeObserver mock). (3) onUnhandledRequest를 Supabase 도메인만 경고하도록 좁혀 카카오 지도 타일/마커(daumcdn) 등 외부 요청 노이즈 제거 | src/mocks/handlers.ts, src/components/prices/PriceTrendChart.tsx, src/test/setup.ts, src/main.tsx | 14 files 45 tests ✅ / npm run build ✅ | 2026-06-11 |
| S10 | Step 5 설계 — 실시간 주변 주유소(Opinet aroundAll.do) + 마커 말풍선 오버레이. brainstorming으로 데이터 소스(Opinet 실시간 반경조회)·UI(CustomOverlay 말풍선) 확정. aroundAll.do 실제 키로 실호출 검증 → 응답 구조 확정(가격 PRICE·거리 DISTANCE·KATEC좌표 포함, 주소·셀프 미제공), fix #14 해소. WGS84→KATEC 임시 역변환 정확도 확인. 구현은 새 세션(plan.md Step 5 작업순서 1~6). **코드 변경 없음(설계·문서만)** | plan/plan.md, fix/fix.md | aroundAll.do 200 + RESULT.OIL 파싱 확인 ✅ | 2026-06-11 |
| S11 | Step 5 구현 — TDD로 `wgs84ToKatec`(역 Molodensky+TM forward) 추가, `around-stations` Edge Function 작성·배포, `useStations` → Edge Function 전환, MSW handler를 `around-stations` 형태로 교체, `KakaoMap` 마커 클릭 말풍선 오버레이(CustomOverlay: 상호/브랜드/현재가/거리)로 전환. 왕복 테스트 3곳(서울/부산/제주) 오차 < 5m. 실호출 확인(서울 좌표 → 실주유소 응답). | src/lib/coord.ts, src/test/coord.test.ts, supabase/functions/around-stations/index.ts, src/hooks/useStations.ts, src/mocks/handlers.ts, src/components/home/KakaoMap.tsx, src/pages/HomePage.tsx | 49/49 tests ✅ / tsc --noEmit ✅ / npm run build ✅ | 2026-06-11 |
| S12 | collect-prices Edge Function Supabase 배포 + 이중 가격 표시(휘발유·경유 병렬 조회·머지) — TDD로 useStations 변경(gasolinePrice/dieselPrice 필드 추가, gasoline+diesel 병렬 fetch 후 ID 기준 머지). StationCard/KakaoMap 말풍선에 두 가격 동시 표시. 선택 유종 행에만 최저가(isLowest) 강조. | supabase/functions/collect-prices/index.ts(배포), src/types/station.ts, src/hooks/useStations.ts, src/mocks/handlers.ts, src/components/home/StationCard.tsx, src/components/home/KakaoMap.tsx, src/test/useStations.test.ts, src/test/StationCard.test.tsx, src/test/StationList.test.tsx | 52/52 tests ✅ / npm run build ✅ | 2026-06-11 |
| S17 | Phase 2-A 실시간 유가 실데이터화 — TDD로 진행. (1) Opinet API 실호출 검증: `avgAllPrice.do`(전국 평균, TRADE_DT/PRODCD/PRICE/DIFF), `avgSidoPrice.do`(시도별 평균, 확인 후 Phase 2-B 보류). (2) `regional_avg` 테이블 migration 적용(Supabase MCP). (3) `_shared/parseAvgPrice.ts` 단일 출처 파서 TDD(6/7 → +kerosene C004 테스트), `src/lib/parseAvgPrice.ts` re-export 배럴. (4) `collect-regional-avg` Edge Function 작성·배포(v1)·실호출: status=success, rows=5, DB 5행 확인. (5) `useAvgPrices` 훅 mock→Supabase 쿼리 TDD(3/3), MSW handlers에 regional_avg 핸들러 추가. (6) `avgPriceFixtures.ts` 삭제(내 변경으로 고아화). (7) data-pipeline-reviewer 검토 후 M1(cron migration CREATE EXTENSION 누락) 수정, 파서 옵셔널 체이닝 추가, kerosene 테스트 보완. pg_cron 스케줄 미결 → fix.md #17. | supabase/migrations/20260612000000_add_regional_avg.sql(신규), supabase/migrations/20260612000001_cron_collect_regional_avg.sql(신규), supabase/functions/collect-regional-avg/index.ts(신규), supabase/functions/_shared/parseAvgPrice.ts(신규), src/lib/parseAvgPrice.ts(신규→re-export), src/hooks/useAvgPrices.ts(mock→Supabase), src/test/parseAvgPrice.test.ts(신규 7테스트), src/test/useAvgPrices.test.ts(신규 3테스트), src/mocks/handlers.ts(regional_avg 핸들러 추가), src/mocks/avgPriceFixtures.ts(삭제) | 81/81 tests ✅ / npm run build ✅ / Edge Function 실호출 rows=5, status=success ✅ | 2026-06-12 |
| S19 | Phase 2-B 프론트엔드 구현 — TDD로 진행. (1) `useNews` 훅 TDD(MSW mock·Supabase REST `news` 테이블 최신 20건): RED→GREEN 2/2. (2) `NewsCard` 컴포넌트 TDD(제목·언론사·경과시간·summary·외부링크): RED→GREEN 4/4. (3) `NewsPage` 교체(placeholder → useNews+NewsCard 리스트+스켈레톤 3개+빈 상태): RED→GREEN 3/3. (4) MSW handlers에 NEWS_MOCK(3건) + `news` 테이블 핸들러 추가. (5) a11y.test.tsx의 NewsPage 테스트에 QueryClientProvider wrapper 추가. | src/hooks/useNews.ts(신규), src/components/news/NewsCard.tsx(신규), src/pages/NewsPage.tsx(교체), src/mocks/handlers.ts(NEWS_MOCK+handler 추가), src/test/useNews.test.tsx(신규 2테스트), src/test/NewsCard.test.tsx(신규 4테스트), src/test/NewsPage.test.tsx(교체 3테스트), src/test/a11y.test.tsx(wrapper 추가) | 93/93 tests ✅ / npm run build ✅ | 2026-06-12 |
| S18 | Phase 2-B 백엔드 구현 — (1) `news` 테이블 migration 적용(Supabase MCP: add_news). (2) `_shared/parseNews.ts` TDD: 정규식 기반 RSS XML 파서(DOMParser 미사용—Deno 호환). `src/lib/parseNews.ts` re-export 배럴. RED→GREEN 4/4. (3) `collect-news` Edge Function 작성·배포(v2—DOMParser→정규식). 실호출: status=success, rows=20, news 테이블 20행. (4) pg_cron migration 적용(cron_collect_news: `0 0,12 * * *`). | supabase/functions/_shared/parseNews.ts(신규), supabase/functions/collect-news/index.ts(신규), src/lib/parseNews.ts(신규→re-export), src/test/parseNews.test.ts(신규 4테스트), Supabase migration: add_news·cron_collect_news | 4/4 parseNews tests ✅ / npm run build ✅ / collect-news 실호출 rows=20 status=success ✅ | 2026-06-12 |
| S20 | 앱 메타데이터 정리 — 사용자가 제공한 `oil_now_icon_128.png` 기반으로 favicon.ico(16/32/48/64 멀티사이즈), PWA 아이콘 128/192/512, 1200×630 OG 이미지 생성. `index.html`에 한글 description, favicon, apple touch icon, Open Graph, Twitter Card 메타 적용. PWA manifest description 및 maskable icon 추가. | index.html, vite.config.ts, public/favicon.ico, public/og-image.png, public/icons/icon-128.png, public/icons/icon-192.png, public/icons/icon-512.png | OG 이미지 육안 확인 ✅ / favicon.ico 4개 이미지 헤더 확인 ✅ / npm run build ✅ / dist 산출물 포함 확인 ✅ | 2026-06-13 |
| S16 | Phase 1 잔여 3 — 브라우저 실물 확인(verify 스킬). Playwright(chromium)로 앱 실제 구동(모바일 390×844 뷰포트, 서울 좌표 Geolocation 권한). (1) 상단 3탭 전환 — `/`(실시간 유가)·`/nearby`(주변 주유소)·`/news`(유가 뉴스) URL 이동 + 활성 표시 확인, 왕복 정상. (2) 실시간 유가 대시보드 — 평균가 카드 3종(휘발유 1,650▲3/경유 1,500▼2/LPG 880 보합) + 7일 추이 차트(Recharts SVG) 렌더. (3) 카카오맵 headless 렌더 정상 + 마커 클릭 → 말풍선 오버레이(S-OIL/1,660·1,510/990m/브랜드) 출현 확인. 뉴스 placeholder 정상. 검증용 playwright devDep·스크립트·스크린샷은 모두 정리(원복). | (코드 변경 없음 — 런타임 관찰만) | 스크린샷 3종 + 마커 클릭 캡처로 PASS ✅ | 2026-06-12 |
| S15 | Phase 1 잔여 2 — Edge Function 재배포 + collect-prices 실호출 검증(fix #14·#16 해소). (1) `around-stations`·`collect-prices`를 `_shared/coord.ts` 단일출처 반영해 재배포(둘 다 version 2, entrypoint에 슬러그 디렉토리 포함 + `_shared/coord.ts` 동봉). (2) collect-prices 실호출: status=success·error 없음·13.9초. (3) 1700→고유 104행 이상 징후로 `siGunGu` 무시 발견 → 직접 실호출로 `area` 파라미터 확정(서울/제주 구분). (4) `siGunGu`→`area` 수정 후 재배포(version 3)·재실행: **104→1,667행, 고유 주유소 1,356개**(유종별 340/340/340/327/320), success 9.3초. | supabase/functions/collect-prices/index.ts(area 수정), Supabase 배포(around-stations v2, collect-prices v3) | collect-prices status=success rows=1667 ✅ / price_snapshots 1667행·1356 주유소 ✅ / collection_logs success ✅ | 2026-06-12 |
| S14 | Phase 1 잔여 1 — `coord.ts` 단일 출처화(fix #11 해소). 좌표 변환 본문을 `supabase/functions/_shared/coord.ts`로 통합. `around-stations`/`collect-prices` 인라인 좌표 코드 제거 후 `../_shared/coord.ts` import. `src/lib/coord.ts`는 `_shared`를 re-export하는 배럴(테스트·parseOpinet import 경로 불변). 검증된 src 버전 그대로 이동이라 동작 동일. **Edge Function 재배포는 잔여 2(collect-prices 실호출)와 함께 예정.** | supabase/functions/_shared/coord.ts(신규), supabase/functions/around-stations/index.ts, supabase/functions/collect-prices/index.ts, src/lib/coord.ts | coord 7/7 ✅ / 전체 18 files 71 tests ✅ / npm run build ✅ | 2026-06-12 |
| S13 | Step 6 QA 패스 — TDD(병렬 Agent A·B). a11y: MainLayout header/main 랜드마크 추가, AvgPriceCard ▲▼ aria-hidden+sr-only 패턴, PricesPage sr-only h1 추가. 색상 대비: text-gray-400→text-gray-500/600(StationCard·PricesPage·NewsPage·AvgPriceCard 전역). Lighthouse 실측 후 추가 수정: AvgPriceCard text-red-500→text-red-700(▲, 3.8→6.9:1), text-gray-400→text-gray-500/600(보합·원 단위). pre-existing 타입 불일치 수정: AvgFuelType 분리(avgPrice.ts), PricesPage FUEL_LABELS 타입 교정. 신규 테스트 17개: a11y.test.tsx(axe 6개), keyboard.test.tsx(6개), touch-targets.test.tsx(5개). Lighthouse 결과: Performance 96 / Accessibility 100 / Best-Practices 100. | src/components/layout/MainLayout.tsx, src/components/prices/AvgPriceCard.tsx, src/pages/PricesPage.tsx, src/pages/NewsPage.tsx, src/components/home/StationCard.tsx, src/types/avgPrice.ts, package.json, src/test/a11y.test.tsx(신규), src/test/keyboard.test.tsx(신규), src/test/touch-targets.test.tsx(신규) | 177/177 tests ✅ / npm run build ✅ / Lighthouse P96·A100·BP100 ✅ | 2026-06-11 |
| S22 | 디자인 개편 D0 — 토큰 기반 다지기. Pretendard 가변폰트 동적 서브셋 CDN 연결, Tailwind v4 `@theme`에 DESIGN.md 색 토큰(프레시 그린 primary/50/200/deep/700, ink/sub/muted/line/surface, price-up/down)·반경(sm6/md14/lg18/xl20) 정의, `--font-sans` Pretendard 지정, body 기본 배경 white·글자 ink 설정. 컴포넌트 파일 미수정(토대만, 화면 큰 변화 없음). | src/index.css | npm run build ✅(CSS 22.7kB 정상 컴파일) / vitest run 101/101 ✅ | 2026-06-14 |
| S29 | 성능 개선 — 코드 스플리팅. App 3개 라우트(PricesPage/HomePage/NewsPage) `React.lazy`로 분리, MainLayout `<main>`의 `<Outlet>`을 `Suspense`로 감싸 헤더·탭 유지. PricesPage의 PriceTrendChart(recharts)도 `lazy`+`Suspense`(200px 플레이스홀더, motion-reduce 가드)로 분리해 랜딩 임계 경로에서 제외. 단일 835KB 번들 → 청크 분리(index 250 / supabase 200 / **recharts 319 지연** / HomePage 34 / NewsPage 3.7 / PricesPage 5.3). Lighthouse Performance 79→82(FCP 2.7→2.3s, LCP 4.7→4.4s). 정리: codex-app-*.log 5개·.claude/worktrees·.superpowers 삭제(gitignore 대상). | src/App.tsx, src/components/layout/MainLayout.tsx, src/pages/PricesPage.tsx | vitest 101/101 ✅ / build 청크 분리 ✅ / Lighthouse P82·A100·BP100 ✅ | 2026-06-14 |
| S28 | 디자인 개편 D3 마감 & 검증. impeccable 오프라인 탐지기(detect.mjs) src 스캔 → 안티패턴 0건. Lighthouse(프로덕션 빌드 preview): **Accessibility 100 / Best-Practices 100 / Performance 79**(기준 ≥70 통과). 성능 최적화: Pretendard 폰트 CSS `@import`(렌더 블로킹 체인) 제거 → index.html `<link>`+`preconnect`로 이동(render-blocking 0건 확인). reduced-motion: NewsPage 스켈레톤에 `motion-reduce:animate-none`. 잔여 성능 병목은 CSR 단일 JS 번들(835KB, recharts/supabase) = 디자인 무관 기존 구조(FCP 2.7s/LCP 4.7s). 코드 스플리팅은 별도 선택 작업으로 분리. | index.html, src/index.css, src/pages/NewsPage.tsx | vitest 101/101 ✅ / build ✅ / Lighthouse A100·BP100·P79 ✅ | 2026-06-14 |
| S27 | 디자인 개편 D2 완료(주변 주유소·뉴스 화면). FilterBar: 유종 칩 파랑→활성 `bg-primary-700 text-white`/비활성 `bg-surface text-sub`, 정렬 select 토큰화. EmptyState: 검색 아이콘(그린 원) + 텍스트 토큰화(gray-400 AA 미달 제거). HomePage: 지도 placeholder `bg-gray-100`→`bg-sheet`, 지역선택 바 `bg-yellow-50`→`bg-primary-50`, 에러 `text-red-500`→`text-red-700`(AA). KakaoMap 말풍선: 가격 강조 빨강(#dc2626)→그린(#0e9f58), 모서리 10→14, 그림자 그린기, Pretendard. NewsCard: 카드 그림자 그린기, 소스 배지 파랑→그린(primary-50/700), 제목/요약/시간 토큰화. NewsPage: `bg-gray-50`→`bg-sheet`, 헤더·스켈레톤 토큰화. | src/components/home/FilterBar.tsx, EmptyState.tsx, KakaoMap.tsx, src/pages/HomePage.tsx, src/components/news/NewsCard.tsx, src/pages/NewsPage.tsx | vitest 101/101 ✅(NewsPage axe 포함) / build ✅ | 2026-06-14 |
| S26 | 디자인 개편 — 상단 영역(브랜드 헤더 + 필 탭) + 배경 토큰화(사용자 피드백 "탭이 흰 배경에 글자만이라 심심"). MainLayout에 브랜드 헤더 추가(그린 로고 마크 + "오일나우" 잉크). TabBar: 밑줄 탭 → **필(pill) 탭**(활성 `bg-primary-700 text-white` 5.2:1 AA, 비활성 `bg-surface text-sub` hover line). 틴트색 토큰화: `@theme`에 `--color-sheet: #eff5f1` 추가 → PricesPage `bg-[#eff5f1]`/`bg-surface`→`bg-sheet`, StationList `bg-surface`→`bg-sheet`로 배경 일관화. **AA:** 활성 필 밝은 그린+흰글자(2.74:1) 미달 → primary-700로 보정. | src/index.css, src/components/layout/MainLayout.tsx, src/components/layout/TabBar.tsx, src/pages/PricesPage.tsx, src/components/home/StationList.tsx | vitest 101/101 ✅(TabBar 필 탭 axe 통과) / build ✅ | 2026-06-14 |
| S25 | 디자인 개편 D2 배경 보정(사용자 피드백 "흰/회색 배경 단순"). 브레인스토밍 visual companion으로 배경 3안 비교 → B "그린 헤더 존" 채택. PricesPage 재구성: 상단 **그린 그라데이션 헤더 띠**(전국 평균 휘발유 인라인: 큰 흰 가격 + 반투명 흰 pill 델타 "어제보다 N원 올랐어요/내렸어요"), 아래 **옅은 그린 틴트 시트(#EFF5F1) rounded-t 오버랩**(-mt-5) 위에 경유·LPG 카드 + 차트 카드(흰색 + 그린기 그림자 `rgba(20,80,50,0.08)`)로 3겹 위계. AvgPriceCard hero variant 제거(인라인 헤더로 대체, 내 변경으로 고아됨) → compact 전용 복귀. smoke.test "전국 평균" 라벨 매칭 유지. | src/pages/PricesPage.tsx, src/components/prices/AvgPriceCard.tsx | vitest 101/101 ✅(반투명 pill+그라데이션 헤더 axe 통과) / build ✅ | 2026-06-14 |
| S24 | 디자인 개편 D2(실시간 유가 화면). AvgPriceCard에 `variant: 'hero'\|'compact'` 추가 — hero=진한 그린 그라데이션 카드(큰 가격+흰 pill 델타), compact=흰 둥근 카드+그림자. PricesPage 재구성: 동일 카드 3개 → 휘발유 그린 히어로 1개 + 경유·LPG 보조 카드 2개(impeccable "동일 카드 그리드" 안티패턴 회피). 월 선택 segmented: 파랑 fill → 연한 그린 선택(`bg-primary-50 text-primary-700`, AA). 추이 차트: LineChart(파랑)→AreaChart(그린 라인+그린 그라데이션 fill). 배경 `bg-surface`, 텍스트 토큰화. **AA:** 흰 텍스트 on `#16B364`=2.74:1 미달 → 히어로 진한 그라데이션+흰 pill로 해결(axe 통과). 제거된 "전국 평균가" h2 때문에 smoke.test 단언을 "전국 평균"(히어로 라벨)으로 갱신. | src/components/prices/AvgPriceCard.tsx, src/pages/PricesPage.tsx, src/components/prices/PriceTrendChart.tsx, src/test/smoke.test.tsx | vitest 101/101 ✅(PricesPage·AvgPriceCard axe 통과) / build ✅ | 2026-06-14 |
| S31 | 적재 파이프라인 진단·수정(fix #20 해소). **진단:** collection_logs·cron.job_run_details·vault.decrypted_secrets·get_edge_function·avgRecentPrice.do 실응답 교차 확인 → 원인 3종 분리(① avgRecentPrice.do에 DIFF 없음 ② cron 6/12·6/13 시크릿 NULL 과거 실패(현재 시크릿 정상) ③ 당일 데이터 Opinet 미제공=정상). **수정:** `_shared/parseAvgPrice.ts`의 `parseRecentPriceResponse`가 유종별 날짜 오름차순 인접일 평균가 차이로 diff 계산(첫날 0, 최신일은 avgAllPrice.do 보정 유지). Edge Function v4 재배포(index.ts 무변경, parseAvgPrice.ts만). cron 동일 경로 수동 트리거로 자동 적재 복구 검증(http 200·success·rows 40), 6/8~6/14 diff 인접일 차이 정확 적재 + 6/14 데이터 등장 확인. | supabase/functions/_shared/parseAvgPrice.ts, src/test/parseAvgPrice.test.ts, Edge Function 배포(collect-regional-avg v4) | tsc ✅ / vitest 101/101 ✅(parseAvgPrice 13개) / 트리거 http 200 success / DB diff 검산 일치 ✅ | 2026-06-14 |
| S30 | 배포 후 사용자 버그 2건 수정. **(이슈1, ui)** 주변 주유소 리스트 클릭 시 미구현 `/stations/:id` placeholder("상세 화면 (Step 5)")로 navigate → 빈 화면. Step 5에서 상세화면→말풍선 전환했으나 리스트 클릭만 죽은 라우트로 남아있던 잔재. HomePage `navigate` 제거→`selectedStationId` 상태로 전환, StationList `onStationClick`이 setState. KakaoMap에 `selectedStationId` prop 추가, 말풍선 로직을 `showOverlay`(useCallback)로 추출(마커 클릭 재사용), 선택 시 `panTo`+말풍선+`scrollIntoView`. App.tsx 죽은 `/stations/:id` 라우트 제거(내 변경으로 고아). **(이슈2, ui/pipeline)** 가격 추이 "월 선택" 탭(6개월)이 일별 누적 데이터 모델과 불일치 → 과거 월 클릭 시 rows=[] → 헤더 가격/델타/카드/차트 전부 사라짐. DB 확인: regional_avg 6/6~6/13 8일치만 존재. 백필 조사: Opinet 일별 시계열 API는 avgRecentPrice.do(최근 7일)뿐 → 소급 불가 확정. 사용자 결정대로 월 선택 제거, useAvgPrices month 파라미터 제거→최근 7일 고정(cutoff 14일 조회 후 trend.slice(-7)), PricesPage 월 탭 UI→"최근 7일" 라벨. diff 대부분 0.00·당일 데이터 부재는 적재 파이프라인 별개 이슈(fix #20)로 분리 기록. | src/pages/HomePage.tsx, src/components/home/KakaoMap.tsx, src/App.tsx, src/hooks/useAvgPrices.ts, src/pages/PricesPage.tsx | tsc --noEmit ✅ / vitest 101/101 ✅ / npm run build ✅ | 2026-06-14 |
| S23 | 디자인 개편 D1 — 공통 컴포넌트 스킨(TabBar·StationCard). TabBar: 파랑→잉크 텍스트+하단 그린 인디케이터(`border-primary`), 비활성 `text-sub`+`border-transparent`(레이아웃 시프트 방지), `font-medium`→`font-semibold`. StationCard: 평면 리스트→둥근 카드(`rounded-lg`+`shadow-[0_2px_10px_rgba(0,0,0,0.05)]`+`bg-white`), `border-b` 제거, hover `bg-primary-50`, 최저가 `text-blue-600`→`text-primary-deep`, 메타/라벨 `text-gray-*`→`text-sub`. StationList: `bg-surface` 배경+행 인셋 `px-3`+`estimateSize` 64→76(카드 간격 확보). **AA 메모:** DESIGN.md muted(#9A9A9E)는 본문 대비 2.8:1로 AA 미달 → 텍스트엔 sub(#6B7280, 4.6:1) 사용(Don'ts와 일치). 최저가 색 변경에 맞춰 StationCard.test·StationList.test 단언 갱신. | src/components/layout/TabBar.tsx, src/components/home/StationCard.tsx, src/components/home/StationList.tsx, src/test/StationCard.test.tsx, src/test/StationList.test.tsx | vitest run 101/101 ✅(axe TabBar 대비 통과) / npm run build ✅ | 2026-06-14 |

---

## 이슈 유형 분류

| 유형 | 설명 |
|------|------|
| `schema` | 데이터베이스 스키마 관련 |
| `pipeline` | 수집 파이프라인 관련 |
| `ui` | 화면 렌더링 관련 |
| `perf` | 성능 관련 |
| `a11y` | 접근성 관련 |
| `test` | 테스트 관련 |
| `scope` | Phase 1 범위 이탈 경고 |

## 심각도 기준

| 심각도 | 기준 |
|-------|------|
| `critical` | Phase 1 완료 기준 미달, 즉시 수정 필요 |
| `major` | 구현 전 결정 필요, 방치 시 재작업 발생 |
| `minor` | 개선 사항, Step 시작 전 확인 권장 |
| `deferred` | Phase 2 이관 결정 |
